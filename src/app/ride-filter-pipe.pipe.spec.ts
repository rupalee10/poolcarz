import { RideFilterPipePipe } from './ride-filter-pipe.pipe';

describe('RideFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RideFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
